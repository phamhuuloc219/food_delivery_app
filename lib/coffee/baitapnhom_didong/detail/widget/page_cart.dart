import 'package:flutter/cupertino.dart';

class PageCartDrink extends StatelessWidget {
  const PageCartDrink({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Đây là Page Cart'),
    );
  }
}
